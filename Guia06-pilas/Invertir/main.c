#include <stdio.h>
#include <stdlib.h>
int main() {
int numero;
struct p;

printf("Ingrese numeros positivos?\n");
scanf("%d",&numero);
while (numero!=-1){
    poner(p,numero);
    scanf("%d",&numero);
}
while (!vacia(p)) print("%d",sacar(p));

return 0;
}
